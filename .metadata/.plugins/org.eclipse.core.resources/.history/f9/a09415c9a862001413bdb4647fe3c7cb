package poof.core;

public class Directory extends FileSystemEntities {
	private final int ENTRY_COST = 8; // cost of the each entry inside the directory
	
	public Directory(String name, User owner) { super(name, owner); }
	
}
